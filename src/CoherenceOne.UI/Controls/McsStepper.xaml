<UserControl x:Class="CoherenceOne.UI.Controls.McsStepper"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="200" d:DesignWidth="400">
  <StackPanel Orientation="Vertical">
    <TextBlock Text="选择能量" FontSize="18" Foreground="{StaticResource Brush.Stroke}" />
    <StackPanel Orientation="Horizontal">
      <RadioButton Content="低" GroupName="Energy" IsChecked="True" Margin="0,0,8,0" />
      <RadioButton Content="中" GroupName="Energy" Margin="0,0,8,0" />
      <RadioButton Content="高" GroupName="Energy" />
    </StackPanel>
    <Border Background="{StaticResource Brush.Note}" CornerRadius="{StaticResource CctCorner}" Padding="8" Margin="0,12,0,0">
      <TextBox Width="240" BorderThickness="0" Background="Transparent" Foreground="{StaticResource Brush.Stroke}" Text="" AcceptsReturn="False" />
    </Border>
    <Button Content="开始 5 分钟" Command="{Binding StartMicroActionCommand}" Margin="0,12,0,0" />
  </StackPanel>
</UserControl>
