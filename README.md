# CoherenceOne

CoherenceOne 是一个基于 .NET 8 + WPF 的专注辅助应用雏形，实现了基础的场景切换、番茄计时、时间线导出与 CCT 主题资源。

## 构建

当前环境未预装 .NET SDK，如需编译请安装 .NET 8 SDK 后执行：

```bash
dotnet restore
dotnet build CoherenceOne.sln
```

## 测试

```bash
dotnet test CoherenceOne.sln
```
